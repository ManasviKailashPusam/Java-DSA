 public static void main(String[]args)
{
            public static int findMiddleElement(Node head)
        { 
            Node head=head;
            int count=0;
            while(temp!=null)
            {
             count++;
             temp=temp.next;
             }
             int midNode=(midNode/2)+1;
    
             while(temp!=null)
             {
               midNode--;
               if(midNode==0)
              {
                 break;
                temp=temp.next;
               }
           }
               return temp.data;
   }

